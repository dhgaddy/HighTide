# litepci — design decisions

litepci is the [enjoy-digital/litepcie](https://github.com/enjoy-digital/litepcie)
LitePCIe controller (TLP + DMA + MSI + Wishbone), generated from
`examples/kcu105.yml` (UltraScale+ `USPCIEPHY`, x4 Gen3, 128-bit data path,
2 DMA channels with monitoring).

The release Verilog (`litepcie_core.v` + `pcie_us_stub.v`) is regenerated by
`designs/src/litepci/dev/gen.sh` against the pinned `litepcie` submodule and the
checked-in `kcu105.yml` config; see `designs/src/litepci/BUILD.bazel` for the
`--define update_rtl=true` path.

## Top-level wrapper

The top module is `litepcie_core` (litex's emitted top name). All non-PHY
boundaries — DMA AXI-Stream channels (`dma0/1_reader/writer_axi_*`), MMAP
master/slave AXI-Lite, MSI IRQs — appear as real top-level ports. PHY-side
signals are the four PCIe lanes (`pcie_rx_p/n`, `pcie_tx_p/n`) plus the
differential refclk (`pcie_clk_p/n`) and async `pcie_rst_n`.

## Xilinx primitives in the generated Verilog

`gen.py` emits three non-portable cells regardless of the target SoC flow:

| Cell | Role | Treatment |
|------|------|-----------|
| `FDPE` | D flip-flop with async preset, 20 instances | Reuse liteeth's synthesizable stub at `designs/src/liteeth/libraries/xilinx/FDPE.v` |
| `IBUFDS_GTE3` | High-speed differential clock buffer feeding the GT block | Synthesizable stub at `designs/src/litepci/libraries/xilinx/IBUFDS_GTE3.v` that passes the (already single-ended in our ASIC build) refclk straight through to `O` and `ODIV2` — mapped via `USE_<PLATFORM>_CELLS` to a tech-specific BUFx2 |
| `pcie_us` | Xilinx UltraScale+ PCIe hard IP (134 ports) | True `(* blackbox *)` — treated as an off-chip macro with a generated placeholder LEF/LIB pair |

Both stubs and the blackbox are auto-derived from the freshly generated
`litepcie_core.v` by `dev/gen_phy_stub.py` (port list) and
`dev/gen_phy_lef.py` (per-platform LEF + dummy timing library). The LEF pin
layer is chosen per platform to match a horizontal-routing layer (M4 on
asap7, metal3 on nangate45, met3 on sky130hd) so the global router can find
access points on the macro's left/right edges.

## `$readmemh` inlining

`gen.py` emits a `$readmemh("litepcie_core_mem.init", mem)` for the LiteX CSR
identification string (~45 bytes of ASCII). Yosys is invoked from a Bazel
sandbox that doesn't have the `.init` file alongside the `.v`, so the read
fails at synthesis time. `dev/inline_readmemh.py` rewrites the call into
explicit `mem[i] = 8'hXX;` initial assignments — no runtime file IO needed.

## Memories — mocked, not mapped (TODO)

Each `litepcie_core.v` build infers 28 memory blocks with five distinct
geometries:

| Width × depth | Bits | Instances | Purpose (approx) |
|--------------:|-----:|----------:|------------------|
| 92  × 256     | 23 552  | 4 | DMA writer/reader table SRAMs |
| 130 × 128     | 16 640  | 2 | DMA buffering FIFOs (small) |
| 130 × 512     | 66 560  | 4 | DMA data FIFOs (medium) |
| 130 × 1024    | 133 120 | 2 | DMA data FIFOs (largest) |
| 230 × 128     | 29 440  | 8 | TLP packetizer/depacketizer buffers |

bsg_fakeram-generated LEF/LIB pairs for all five sizes are committed under
each `designs/<platform>/litepci/sram/`, but the **inferred reg-array
memories are not yet patched to explicit fakeram instances** the way
liteeth does via `designs/src/liteeth/patch/*.patch`. To unblock the
first build, every platform sets:

```
"SYNTH_MOCK_LARGE_MEMORIES": "1",
"SYNTH_MEMORY_MAX_BITS":     "4096",
```

…so yosys collapses each oversized memory to a single row. The resulting
flow exercises end-to-end synthesis → P&R → final reporting, but the
area/timing numbers under-estimate what the real ~1 Mb of on-chip memory
would contribute. **TODO: write a generic LiteX-style `reg [W:0] storage[0:D];`
→ `fakeram_…` patcher in `dev/gen.sh`** (analogous to liteeth's per-variant
hand-written patches but generated from the inferred memory map).

## Per-platform notes

### asap7

- `CORE_UTILIZATION = 40`, `PLACE_DENSITY = 0.55`, `MACRO_PLACE_HALO = 5 5`.
- Single macro (the pcie_us blackbox) sits left-of-center; nothing else
  needs RTLMP intervention.
- Clean DRC, mocked memories → no register-to-register paths timed.

### nangate45

- `CORE_UTILIZATION = 35`, `PLACE_DENSITY = 0.5`, `MACRO_PLACE_HALO = 15 15`.
- First attempt placed pcie_us pins on metal4 (VERTICAL routing layer),
  causing `[ERROR DRT-0073] No access point for pcie_us/<pin>` at GRT.
  Fix: re-emit the pcie_us LEF with pins on metal3 (HORIZONTAL) — see
  `dev/gen_phy_lef.py:PLATFORMS["nangate45"]`.

### sky130hd

- `CORE_UTILIZATION = 30`, `PLACE_DENSITY = 0.45`, `MACRO_PLACE_HALO = 20 20`.
- Same met4→met3 fix as nangate45.
- sky130hd has no `CORE ANTENNACELL` diode, so detail_route's antenna
  repair plateaus at a handful of unfixable violations (3 net, 3 pin in
  the first cached build); ORFS gives up after the configured retry budget
  and the flow reaches `6_final` cleanly.

## `GDS_ALLOW_EMPTY`

`hightide_design()` defaults to `fakeram.*`. Each platform extends that to
`fakeram.*|pcie_us` so OpenROAD doesn't insist on a GDS body for the PCIe
hard-IP blackbox.
